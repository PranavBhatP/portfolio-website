/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized:true,
    },
    basePath: "/portfolio-website",
    output: "export",
    reactStrictMode: true,
};
  

export default nextConfig;
